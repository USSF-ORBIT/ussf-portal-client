mutation editWeatherWidget($_id: OID!, $title: String, $zipcode: String) {
  editWeatherWidget(_id: $_id, title: $title, zipcode: $zipcode) {
    _id
    title
    coords {
      lat
      long
      forecastUrl
      hourlyForecastUrl
      city
      state
      zipcode
    }
  }
}
