/**
 * @jest-environment jsdom
 */

import { localResolvers } from './resolvers'

describe('getCollections', () => {
  it('gets all collections', async () => {
    console.log('what exactly is local Resolvers')
  })
})
