query GetUser($getUserId: String!) {
  getUser(id: $getUserId) {
    FirstName
    LastName
    DOD_ID
    Grade
    MajCom
    DutyTitle
    Country
    BaseLoc
    OrgType
    Rank {
      Title
      Abbreviation
      Grade
      GradeId
    }
    EOPDate
    UserType
    lastModifiedAt
  }
}
