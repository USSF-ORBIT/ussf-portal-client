name: Lint PR

on:
  pull_request_target:
    types: [opened, edited, synchronize]
    branches:
      - main

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Lint pr title
        uses: amannn/action-semantic-pull-request@25e3275e19cd5b3577a99478084255065002897c # renovate: tag=v3.4.2
        with:
          wip: true
          types: |
            build
            chore
            ci
            dev
            docs
            feat
            fix
            perf
            refactor
            revert
            style
            test

        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
