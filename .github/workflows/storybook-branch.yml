name: Netlify Storybook Branch Deploy

on:
  pull_request:
    types:
      - labeled

jobs:
  storybook-branch-deploy:
    runs-on: ubuntu-latest
    if: github.event.label.name == format('type{0} storybook component', ':')
    steps:
      - name: Check out repository
        uses: actions/checkout@ac593985615ec2ede58e132d2e21d2b1cbd6127c

      - name: Trigger Netlify build
        id: deploy-storybook-to-netlify
        uses: jsmrcaga/action-netlify-deploy@d885259ff7079f1e030aef5aaca95269916aea08 # v1.8.1
        with:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_DEPLOY_TO_PROD: false
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
          build_directory: 'storybook-static'
          build_command: 'yarn storybook:build'
          node_version: 14.19.1
        continue-on-error: false

      - name: Post Storybook URL
        uses: thollander/actions-comment-pull-request@4159e62bc85fec21089efb305e83a9e7113074e8 # v2.3.0
        with:
          GITHUB_TOKEN: '${{github.token}}'
          message: '🎉 View this branch in Storybook - [${{ steps.deploy-storybook-to-netlify.outputs.NETLIFY_PREVIEW_URL }}](${{ steps.deploy-storybook-to-netlify.outputs.NETLIFY_PREVIEW_URL }})'
